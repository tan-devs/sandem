/**
 * Vitest client setup file.
 *
 * This file runs once before the unit test suite (configured via
 * `test.setupFiles` in `vitest.config.ts`).
 *
 * Use it to register global test behavior for client-side tests, such as:
 * - browser/API polyfills
 * - shared mocks/stubs
 * - custom matchers
 * - global test hooks
 */
